{
  "name": "odw-daemon",
  "version": "0.1.0",
  "description": "Local orchestration daemon for open-dynamic-workflows: sandboxed scripts, 100+ concurrent agents, crash-resumable state.",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "odw-daemon": "src/cli.js"
  },
  "scripts": {
    "test": "node --test test/",
    "coverage": "c8 --check-coverage --lines 80 --include \"src/**\" --exclude \"src/cli.js\" node --test test/"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "better-sqlite3": "^11.10.0",
    "commander": "^14.0.2",
    "express": "^5.2.1",
    "odw-core": "^0.1.0",
    "p-queue": "^9.3.0",
    "quickjs-emscripten": "0.32.0",
    "ws": "^8.21.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
