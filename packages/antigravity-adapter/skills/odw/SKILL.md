---
name: odw
description: Dynamic multi-agent workflows — plan first, then orchestrate parallel agents with verification via the local odw daemon. Trigger on "workflow" or "ultracode" requests.
---

# Open Dynamic Workflows (Antigravity)

(P4 fills the full skill body. Same canonical SKILL.md format as the Codex
adapter — install path is ~/.gemini/skills/odw/ instead.)
